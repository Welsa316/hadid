# Postgres connection string.
# On Railway: add a Postgres database to the project, then reference its
# connection variable here (Railway exposes it as DATABASE_URL automatically).
# Locally: point this at your dev Postgres.
DATABASE_URL=postgresql://localhost:5432/hadid_sync

# Port the server listens on. Railway sets this automatically in production.
PORT=8787
